'use client' //define client side code

import { CreateUserForm } from "@/components/createUserForm";

export const CreateUser = () => {
    return (
        <div>
            <h1>Welcome to pg testing</h1>
            <CreateUserForm />
        </div>
    );
}